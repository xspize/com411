#Simple Set Program
def observed():
# creates a set followed by a list of names 
    observations = {"Flying Car", "Sky Scraper", "Laser" , "Dome" }
# returns the set
    return observations
    
def run():
#stores the value of the set in a local variable
    return_results = observed()
#prints the results from the other function that is stored in a local variable
    print(return_results)

#runs the function
run()
